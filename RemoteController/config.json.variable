{
  "ServerPort": "${REMOTE_CONTROLLER_SERVER_PORT}",
  "RemoteSwitchHost": "${REMOTE_CONTROLLER_HOST}",
  "RemoteSwitchPort": "${REMOTE_CONTROLLER_PORT}",
  "RemoteSwitchUID": "${REMOTE_CONTROLLER_UID}"
}
